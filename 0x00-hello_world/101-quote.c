#include <stdio.h>

/**
 * main - Entry point
 * description: print with proper grammar, but the outcome is a piece of art
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with a proper grammar, but outcome is a piece of art,\n");
			return (0);
}
